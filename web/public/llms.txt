# equation.io

> Free in-browser graphing calculator (WebGL). Plots 2D curves, inequality
> regions, scalar fields, vector fields and ODEs, complex functions (including
> domain coloring, conformal maps and escape-time fractals), 3D surfaces,
> parametric curves/surfaces, plane geometry (named draggable points,
> segments, polylines, vector arrows, polygons, squares, circles), symbolic
> derivatives and integrals,
> and random variables with shaded probabilities. No account, no server round-trips: the graph state
> lives in the URL fragment, so you can construct a link that opens the app
> with any set of equations already rendered.

## Deep links

Format:

    https://equation.io/g/<equation>;<equation>;...

- Percent-encode each equation (JavaScript `encodeURIComponent`), then
  additionally escape ( ) ! ' * as %28 %29 %21 %27 %2A — chat apps cut
  auto-linked URLs at bare parentheses, silently truncating the graph.
- Encoded equations are joined with `;`, and that literal `;` is only ever the
  row separator (a reader also accepts `%3B` there, since clients re-encode
  it). A `;` that belongs to an equation — inside quoted text, like
  `p[p.city == "a;b"]` or `open("sales;2026.csv")` — is encoded TWICE, as
  `%253B`, so the two can never be confused: the payload encodes the quotes
  too, and a singly-encoded one comes back as a row separator, splitting the
  equation in half.
- Every equation gets its own row and color. Definitions (constants,
  functions, coordinate fields) are rows too — order does not matter.
- `/g/` links unfurl with a rendered preview image (og:image) in chat apps
  and social cards; the image itself is served at
  `https://equation.io/api/og/<same payload>`. An equivalent fragment form,
  `https://equation.io/#<same payload>`, also opens the graph and keeps the
  equations out of server logs, but gets no preview card.

Examples (paste-ready):

- [Parabola](https://equation.io/#y%3Dx%5E2): `y = x^2`
- [Circle](https://equation.io/#x%5E2%2By%5E2%3D4): `x^2 + y^2 = 4`
- [Sine + tangent line](https://equation.io/#f(x)%3Dx%5E3-2x;g(x)%3Dd%2Fdx%20f(x);a%3D1;y%3Df(x);y%3Df(a)%2Bg(a)(x-a)): `f(x) = x^3 - 2x; g(x) = d/dx f(x); a = 1; y = f(x); y = f(a) + g(a)(x-a)`
- [Slider-controlled wave](https://equation.io/#a%3D2;y%3Dsin(a%20x)%2Fa): `a = 2; y = sin(a x)/a`
- [Fourier series](https://equation.io/#N%20%3D%208;y%20%3D%202%20sum%5Bn%3D1..N%5D%20(-1)%5E(n%2B1)%20sin(n%20x)%2Fn): `N = 8; y = 2 sum[n=1..N] (-1)^(n+1) sin(n x)/n` — drag N for more harmonics
- [Animated ripples](https://equation.io/#sin(x%5E2%2By%5E2-4t)%2F2): `sin(x^2+y^2-4t)/2`
- [3D saddle](https://equation.io/#z%3D(x%5E2-y%5E2)%2F4): `z = (x^2 - y^2)/4`
- [Torus (parametric 3D)](https://equation.io/#(cos(2pi%20u)(2%2Bcos(2pi%20v))%2C%20sin(2pi%20u)(2%2Bcos(2pi%20v))%2C%20sin(2pi%20v))): `(cos(2pi u)(2+cos(2pi v)), sin(2pi u)(2+cos(2pi v)), sin(2pi v))`
- [Complex dipole field](https://equation.io/#ln(w-2)-ln(w%2B2)): `ln(w-2) - ln(w+2)`
- [Domain coloring](https://equation.io/#domain((w%5E3%20-%201)%2Fw)): `domain((w^3 - 1)/w)`
- [Conformal grid image](https://equation.io/#conformal(w%5E2%2F4)): `conformal(w^2/4)`
- [Mandelbrot set](https://equation.io/#iter(z%5E2%20%2B%20w)): `iter(z^2 + w)`
- [Julia set](https://equation.io/#iter(z%5E2%20-%200.7269%20%2B%200.1889i)): `iter(z^2 - 0.7269 + 0.1889i)`
- [Vector field](https://equation.io/#(-y%2C%20x)): `(-y, x)` — drawn as animated streamlines
- [Pendulum phase portrait](https://equation.io/#(x'%2C%20y')%20%3D%20(y%2C%20-sin(x))): `(x', y') = (y, -sin(x))`
- [Slope field](https://equation.io/#y'%20%3D%20x%20-%20y): `y' = x - y`
- [Polar cardioid](https://equation.io/#r%3Dsqrt(x%5E2%2By%5E2);theta%3Datan2(y%2Cx);r%3D2(1%2Bcos(theta))): `r = sqrt(x^2+y^2); theta = atan2(y,x); r = 2(1 + cos(theta))`
- [Circle through a draggable point](https://equation.io/#C%20%3D%20(0%2C%200);P%20%3D%20(2%2C%201);circle(C%2C%20%7CP%20-%20C%7C);segment(C%2C%20P)): `C = (0, 0); P = (2, 1); circle(C, |P - C|); segment(C, P)`
- [Triangle: a side and its angles](https://equation.io/#A%20%3D%20(-2%2C%20-1);B%20%3D%20(3%2C%20-0.5);C%20%3D%20(0.5%2C%202.5);polygon(A%2C%20B%2C%20C);distance(A%2C%20B);angle(B%2C%20A%2C%20C)%20180%2Fpi;angle(B%2C%20A%2C%20C)%20%2B%20angle(C%2C%20B%2C%20A)%20%2B%20angle(A%2C%20C%2C%20B)): `A = (-2, -1); B = (3, -0.5); C = (0.5, 2.5); polygon(A, B, C); distance(A, B); angle(B, A, C) 180/pi; angle(B, A, C) + angle(C, B, A) + angle(A, C, B)` — distance() and angle() are numbers, so the bare rows read out "= value" and follow the points as you drag; the angle sum stays π (−π once the triangle turns clockwise: angles are signed)
- [Vector sum (parallelogram rule)](https://equation.io/#A%20%3D%20(3%2C%201);B%20%3D%20(1%2C%202);vector(A);vector(B);vector(A%20%2B%20B);polyline(A%2C%20A%20%2B%20B%2C%20B)): `A = (3, 1); B = (1, 2); vector(A); vector(B); vector(A + B); polyline(A, A + B, B)` — vector() draws arrows (from the origin with one argument), polyline() the open path completing the parallelogram; drag A or B
- [Thébault's theorem (squares on a parallelogram)](https://equation.io/#A%20%3D%20(0%2C%200);B%20%3D%20(4%2C%200.5);D%20%3D%20(1%2C%202.5);C%20%3D%20B%20%2B%20D%20-%20A;polygon(A%2C%20B%2C%20C%2C%20D);square(B%2C%20A);square(C%2C%20B);square(D%2C%20C);square(A%2C%20D);P%20%3D%20midpoint(A%2C%20B)%20-%20perp(B%20-%20A)%2F2;Q%20%3D%20midpoint(B%2C%20C)%20-%20perp(C%20-%20B)%2F2;R%20%3D%20midpoint(C%2C%20D)%20-%20perp(D%20-%20C)%2F2;S%20%3D%20midpoint(D%2C%20A)%20-%20perp(A%20-%20D)%2F2;polygon(P%2C%20Q%2C%20R%2C%20S)): `A = (0, 0); B = (4, 0.5); D = (1, 2.5); C = B + D - A; polygon(A, B, C, D); square(B, A); square(C, B); square(D, C); square(A, D); P = midpoint(A, B) - perp(B - A)/2; …; polygon(P, Q, R, S)` — drag A, B or D; the square centers stay a square
- [Normal probability](https://equation.io/#m%20%3D%201;s%20%3D%200.5;X%20~%20Normal(m%2C%20s);P(0%20%3C%20X%20%3C%202)): `m = 1; s = 0.5; X ~ Normal(m, s); P(0 < X < 2)` — the density with the area between 0 and 2 shaded; drag m or s and the probability follows
- [Swinging pendulum (integrated state)](https://equation.io/#th'%20%3D%20om;om'%20%3D%20-sin(th)%20-%20om%2F8;th(0)%20%3D%203;bob%20%3D%20(sin(th)%2C%20-cos(th));segment((0%2C%200)%2C%20bob);bob): `th' = om; om' = -sin(th) - om/8; th(0) = 3; bob = (sin(th), -cos(th)); segment((0, 0), bob); bob` — th is the angle, om the angular velocity; segment() draws the rod, the named point draws the bob
- [Double pendulum](https://equation.io/#g%20%3D%209.8;L1%20%3D%201;L2%20%3D%201;m1%20%3D%201;m2%20%3D%201;M%20%3D%20%5B((m1%2Bm2)%20L1%2C%20m2%20L2%20cos(th_1%20-%20th_2))%2C%20(L1%20cos(th_1%20-%20th_2)%2C%20L2)%5D;f%20%3D%20(-m2%20L2%20om_2%5E2%20sin(th_1%20-%20th_2)%20-%20(m1%2Bm2)%20g%20sin(th_1)%2C%20L1%20om_1%5E2%20sin(th_1%20-%20th_2)%20-%20g%20sin(th_2));th'%20%3D%20om;om'%20%3D%20solve(M%2C%20f);th(0)%20%3D%20(2.5%2C%202.4);b1%20%3D%20(L1%20sin(th_1)%2C%20-L1%20cos(th_1));b2%20%3D%20b1%20%2B%20(L2%20sin(th_2)%2C%20-L2%20cos(th_2));segment((0%2C%200)%2C%20b1);segment(b1%2C%20b2);b1;b2): `M = [((m1+m2) L1, m2 L2 cos(th_1 - th_2)), (L1 cos(th_1 - th_2), L2)]; f = (…); th' = om; om' = solve(M, f); th(0) = (2.5, 2.4); …` — the Lagrangian form M(θ)ω′ = f: th and om are 2-vector states with components th_1, th_2; solve() is Cramer's rule; segment()s between named bob points draw the linkage
- [Orbit (vector gravity)](https://equation.io/#r'%20%3D%20vel;vel'%20%3D%20-9%20r%2F%7Cr%7C%5E3;r(0)%20%3D%20(2%2C%200);vel(0)%20%3D%20(0%2C%201.5);segment((0%2C%200)%2C%20r);r;(0%2C%200)): `r' = vel; vel' = -9 r/|r|^3; r(0) = (2, 0); vel(0) = (0, 1.5); segment((0, 0), r); r; (0, 0)` — vector states with point arithmetic; the bare state name draws as a moving point
- [Matrix phase portrait](https://equation.io/#a%20%3D%20-1;b%20%3D%20-1%2F4;A%20%3D%20%5B(0%2C%201)%2C%20(a%2C%20b)%5D;(x'%2C%20y')%20%3D%20A%20(x%2C%20y)): `a = -1; b = -1/4; A = [(0, 1), (a, b)]; (x', y') = A (x, y)` — a linear system as its literal matrix, sliders in the entries
- [Poisson: strict versus non-strict](https://equation.io/#m%20%3D%204;X%20~%20Poisson(m);P(X%20%3C%203);P(X%20%3C%3D%203);P(X%20%3D%203)): `m = 4; X ~ Poisson(m); P(X < 3); P(X <= 3); P(X = 3)` — a discrete variable draws its pmf as stems; each P(…) row highlights exactly the stems it counts, so `<` stops at 2, `<=` takes the stem at 3, and `P(X = 3)` is that one stem
- [Two dice](https://equation.io/#X%20~%20DiscreteUniform(1%2C%206);Y%20~%20DiscreteUniform(1%2C%206);S%20%3D%20X%20%2B%20Y;P(S%20%3E%3D%2010);P(X%20%3E%20Y);P(X%20%3E%3D%20Y);E(S)): `X ~ DiscreteUniform(1, 6); Y ~ DiscreteUniform(1, 6); S = X + Y; P(S >= 10); P(X > Y); P(X >= Y); E(S)` — arithmetic over discrete variables is exact: the sum's pmf is the triangle on 2…12, drawn as stems; `P(X > Y)` = 15/36 and `P(X >= Y)` = 21/36 differ by the ties, `P(X = Y)`
- [Central limit theorem](https://equation.io/#X1%20~%20Uniform%280%2C%201%29;X2%20~%20Uniform%280%2C%201%29;X3%20~%20Uniform%280%2C%201%29;X4%20~%20Uniform%280%2C%201%29;S%20%3D%20X1%20%2B%20X2%20%2B%20X3%20%2B%20X4;Z%20~%20Normal%282%2C%20sqrt%281%2F3%29%29;P%28S%20%3E%203%29): `X1 ~ Uniform(0, 1); X2 ~ Uniform(0, 1); X3 ~ Uniform(0, 1); X4 ~ Uniform(0, 1); S = X1 + X2 + X3 + X4; Z ~ Normal(2, sqrt(1/3)); P(S > 3)` — the sum of four uniforms hugs the matching normal
- [Conditional random variable](https://equation.io/#X%20~%20Normal%280%2C%201%29;Y%20%3D%20%7BX%20%3E%200%3A%20X%5E2%2C%201%7D;P%28Y%20%3E%200.5%29;P%28Y%20%3E%20X%29): `X ~ Normal(0, 1); Y = {X > 0: X^2, 1}; P(Y > 0.5); P(Y > X)` — a piecewise transform of X, with probabilities of derived and joint events

## Share links with preview images (/g/)

The same payload also works as a path, and this form unfurls with a rendered
preview image (og:image) in chat apps and social cards:

    https://equation.io/g/<equation>;<equation>;...

Prefer the `/g/` form when the link will be posted somewhere that shows link
previews; prefer the `#` form otherwise (it keeps the equations out of server
logs). Both open the same app with the same graph. The preview image itself is
served at `https://equation.io/api/og/<same payload>`.

## MCP server

An MCP endpoint (Streamable HTTP) lives at:

    https://equation.io/mcp

Tools: `encode_graph_url` (validates each equation through the app's own parser,
returns url/share_url plus per-row classification or errors, with text-only MCP responses. Share links still provide a PNG preview; rows the preview renderer cannot draw are
listed in `preview_omits` with the reason — the live app renders more than
the static preview, so an omission never means the row failed) and
`decode_graph_url` (decodes an existing link back into its equation rows so you can
edit a user's graph and build a new link). This document is also served as
the server's `syntax` MCP resource, so the full reference can be pulled on
demand instead of living in a tool description. No authentication required.

Use `show_graph` to display the interactive graph directly in an MCP Apps host
such as ChatGPT web. It accepts the same `equations` array and returns per-row
validation and share links plus an associated UI resource. It does not include
`preview` / `preview_omits` — those describe the share-link card on
`encode_graph_url`, not the widget.
The widget supports editing, sliders, zoom, and 3D rotation; edits update the
model's context with the complete current equations and share_url. Preserve
those edited rows on follow-up requests. Use `encode_graph_url` for validation
or link-only requests. Clients without MCP Apps support can open share_url.

## Expression syntax

- Operators: `+ - * / ^`, parentheses, unary minus. Multiplication is
  implicit: `2x`, `x y`, `2cos(t)`, `a(x-1)` all work.
- Absolute value bars: `|x|`, including nested/multiplied forms like `2|x-1|`.
- Unicode: Greek letters are ordinary names (`θ = 1` makes a θ slider; `Δx`
  is a name like `dx`), superscript digits are exponents (`x³` is `x^3`,
  `x⁻²` is `x^(-2)`), and subscript digits are the `_` subscript syntax in
  its unicode spelling — `T₀` IS `T_0`, so `r₁` reads a vector state's `r_1`
  component and `a₃` a sequence's third term. `·`, `⋅`, `×`, `÷`, `−`, `≤`,
  `≥`, `≠` mean their ASCII counterparts. π and τ never join a longer name,
  so `2πr` is 2·π·r. In the editor, typing `\pi`, `\theta`, `\nabla`, …
  inserts the symbol itself, and a `\` before any built-in function name
  simply drops (`\sin(x)` → `sin(x)`, `\trail` → `trail`), so `\` also works
  as a function search.
- Constants: `pi`, `tau`, `e` (π and τ also parse directly). Imaginary unit
  `i` in complex expressions.
- Functions: sin cos tan asin acos atan atan2(y,x) sinh cosh tanh sech coth
  asinh acosh atanh sqrt abs exp ln (natural) log (base 10) floor ceil round
  min max mod sign fract sinc gamma factorial, complex helpers re im arg conj,
  and point helpers dot cross perp midpoint unit. Function names are
  case-insensitive: `Sin(x)`, `SQRT(x)` and `sin(x)` are the same call. Always
  use parentheses for arguments — write `sin(x)`, not `sin x`.
- Factorial: postfix `!` — `5!` is 120, `x!` plots as Γ(x+1), `2^3!` is 2^6.
  Non-integers use the Gamma function; negative integers are undefined.
- Piecewise: `{x < 0: -x, x >= 0: x^2}` — `cond: value` cases tried in order,
  with an optional last bare value as the default (`{0 < x < 1: 1, 0}`).
  Conditions are inequalities and may chain. With no default the value is
  undefined wherever no case holds and nothing is drawn there, which makes
  one case a domain restriction: `y = {0 < x < 2: x^2}` plots the parabola
  on (0, 2) only. It works in any row kind — `x^2 + y^2 = {x > 0: 16}` is a
  half circle, `(2cos(u), {sin(u) > 0: 2sin(u)})` cuts a parametric curve.
- Derivatives: `d/dx (x^3)` differentiates symbolically; higher order
  `d^2/dx^2 (...)`; works for any single-letter variable, e.g. `d/dq (q^2)`.
- Sums and products: `sum(n=1..N, sin(n x)/n)` and `prod(k=1..N, k)`, also
  written `Σ`/`Π`. The bracket form `sum[n=1..N] sin(n x)/n` takes everything
  after it in the product chain as the body (`2 sum[n=1..N] ...` keeps the 2
  outside). Bounds must be numbers or constants defined in another row (not
  `t`), because the sum is expanded symbolically before compiling — which makes
  `N` a slider that adds terms as you drag it.
- Integrals: `int[a..b] f(x) dx` (definite) and `int(f(x) dx)`
  (antiderivative), also written `∫`. The trailing `dx` names the variable;
  any letter works, so `y = int[0..x] exp(-t^2) dt` plots a function of x,
  and bounds may use sliders, t, or x. Integration is symbolic where a
  verified antiderivative exists — polynomials and rational functions
  (exactly), the elementary families, u-substitution, integration by parts,
  Gaussians via erf — and falls back to Gauss–Legendre quadrature expanded
  in place otherwise, so non-elementary rows like `y = int[0..x] sin(t)/t dt`
  (Si) still plot. A row whose integral resolves to a constant shows the
  numeric value as a readout — and when the row is exactly one definite
  integral of a real integrand (`int[0..b] sin(x) dx`; bounds may be sliders,
  states, t or `±inf`), it also shades the area between the integrand and the
  axis over [a, b], in 2D: parts that add to the value in the row color,
  parts that subtract in its complement (so reversed bounds swap the tints).
  The dx variable runs along the horizontal axis whatever its letter; add
  `y = sin(x)` as its own row to see the curve beyond [a, b]. A coefficient,
  a sum of integrals, an iterated integral or a definition
  (`A = int[0..1] x^2 dx`) stays a plain number. Classic notations work: `int[0..1] dx/(1+x^2)`,
  and iterated integrals pair inside-out: `int[0..1] int[0..y] x dx dy`.
  Bounds may be `±inf` (or `∞`): `int[-inf..x] exp(t) dt` is exp(x),
  `int[-inf..inf] exp(-x^2) dx` reads √π, and `int[-inf..x] normalpdf(t, 0, 1) dt`
  is the normal cdf. Divergent limits are refused (the closed form is checked
  against real quadrature), not misreported.
- Lists: `[3, 1, 4]`, or ranges `[1..20]`, `[0, 0.5..10]` (the first two
  elements set the step), `[10..1]` (counts down). Range bounds are numbers
  or constants defined in another row, like Σ bounds. List math broadcasts
  elementwise — `2L + 1`, `sin(L)`, `L^2`. A list is a variable that ranges
  over its values: the row is evaluated once per instance, so every use of
  the SAME list moves together (zips) — `s = [1..50]/5` then `(s, sin(s))`
  is a scatter of samples, and lists derived from `s` (`q = s^2`,
  `s[s > 2]`, `a_[s]`) stay paired with it. Lists with DIFFERENT origins are
  independent and cross: each `[…]` literal is its own origin, so
  `([0,1],[0,1],[0,1])` is the 8 corners of a cube, `[1,2] + [10,20,30]` has
  6 elements, and two separately defined lists `(a, b)` make a grid. Columns
  of one data file share its rows, so `(data.x, data.y)` pairs up. To pair
  literal values write the points out: `[(1,4), (2,5), (3,6)]`.
- List reductions: `mean(L)`, `total(L)`, `count(L)`, `min(L)`, `max(L)`,
  `stdev(L)` (sample), `median(L)`, `sort(L)`. A row that reduces to a
  constant shows the numeric value as a readout. mean/total/count/min/max
  expand symbolically, so elements may animate (`mean([sin(t), 2])`);
  stdev/median/sort need constant elements. Over a column, a missing cell is
  left out of every reduction (as it is of `hist`) except `count(L)`, which
  answers how many rows there are.
- `hist(L)` bins a list into a bar chart — `hist(L, 40)` sets the bin count
  (default ≈ √n, between 5 and 60). It is a whole row, not a value, and the
  one thing you may do to it is scale it: the plane has a single scale for
  both axes, so counts in the thousands need `hist(L, 40) / 800` to share a
  view with values in the units. Missing values are left out.
- List indexing is 1-based, on named lists: with `L = [5, 6, 7]` in a row
  above, `L[2]` is 6 and `L[k]` takes k from a slider. Only a named list
  indexes — `x[2]` is plain multiplication (2x).
- Data columns: with `person = open("people.csv", 3a7f9c1d2e4b)` in a row
  above (see Definitions), `person.age` is a list of that column's numbers.
  Everything lists can do applies: `(person.age, person.height)` is a
  scatter, `mean(person.age)` a readout, `person.age[2]` one value. Naming
  works throughout — `half = person.age / 2`, `P = (person.age, person.height)`
  — and a named scatter is still a scatter. Only the
  device that dropped the file has its data — the link carries the file's
  name and hash, not its bytes.
- Filters keep the elements a comparison selects: `L[L > 2]`,
  `person.height[person.age >= 18]` (one column chosen by another — rows stay
  aligned), chains, `person.age[30 <= person.age < 40]`, and text,
  `person.height[person.city == "NYC"]` (`!=` too, and either quote).
  Filtering a whole file names a new one,
  `adults = person[person.age >= 18]`, whose columns work like any others
  (`(adults.age, adults.height)`). A filter settles when the row compiles, so
  it may use constants and sliders but not t; a comparison with a missing
  cell is false, so filtering also drops gaps.
- Text columns are lists of text. They are not plottable and have no
  arithmetic directly. `person.name.length` returns a numeric list of character
  counts (Unicode code points) and plots one count per row. Missing cells stay
  gaps; counts use the parsed, trimmed text. Use it in arithmetic, reductions,
  histograms, and filters: `hist(person.name.length)` or
  `long_names = person[person.name.length > 10]`. `count(person.name)` still
  counts rows. `==` and `!=` exist only for filters: an equation still takes
  a single `=`.
- Comments: a row starting with `#` (e.g. `# tangent lines`) plots nothing; in
  the app it renders as a heading whose group (the rows until the next `#` row)
  can be collapsed. Use them to label sections of longer documents.

## Variables (what a row means)

- `x`, `y`: the 2D plane. `y = f(x)`, or any implicit equation in x and y
  (`x^2+y^2=4`), renders as a curve.
- `z`: using z switches to 3D. `z = sin(x)cos(y)` is a surface;
  `x^2+y^2+z^2=9` is an implicit 3D surface.
- `t`: time in seconds. Any row mentioning `t` animates, e.g.
  `y = sin(x - 2t)`.
- `u`, `v`: parametric parameters, each ranging over (0, 1).
- `w`: the complex plane point x + iy. A complex-valued expression in `w`
  (e.g. `ln(w-2) - ln(w+2)`) draws field lines and equipotentials. Wrapping it
  in `domain(…)` or `conformal(…)` picks a different view of the same function.
- `z` inside `iter(…)`: the iterate being fed back through the step. Outside
  `iter`, `z` is the 3D axis as above.

## Row types

- Curve: an equation in x and y — `y = x^2`, `(x^2+y^2)^2 = 8(x^2-y^2)`.
- Region: inequalities, including chains — `y < x/2 + 1`,
  `4 <= x^2 + y^2 <= 9`. Non-strict comparisons draw a solid boundary.
- Scalar field: a bare expression in x, y — `sin(x)cos(y)` — shaded by value.
- Value: a bare expression with no plot coordinate — `2+2`, `sqrt(a)` with a
  slider a, `|A - B|` between named points, `int[0..1] x^2 dx`, `sin(t)` —
  draws nothing (a lone definite integral shades its area) and reads out `= 4` (or `≈ 1.41421`) under the row, live as
  sliders, states and t move. The tool result reports it as kind `value`
  with the number in `value`. A number is not assumed to mean `y = number`:
  write `y = 4` for the horizontal line. (A bare expression in x alone,
  like `sin(x)`, still plots as y = sin(x).)
- Complex point: a constant complex expression such as `1+2i` draws at (1, 2).
  Constants and t may animate its real and imaginary components.
- Complex roots: `w^3 = 1` draws three solution points; equations in w become
  two real constraints. This is a numerical search over the visible window,
  with exact labels for rational/quadratic polynomial factors and a defining-
  polynomial label for remaining algebraic roots. The optional certification
  control proves completeness only when every region in its search box is settled.
- Complex path: a complex-valued expression whose only variable is u (constants,
  sliders and t aside) draws the path it traces over u in (0, 1), at
  (re, im) — the same plane as complex points and roots. `exp(i 2 pi u)` is
  the unit circle; with `f(w) = w^2 + w`, `f(exp(i 2 pi u))` is its image
  under f. Supported: arithmetic, powers, exp ln log sqrt, sin cos tan sinh
  cosh tanh, re im abs arg conj, user functions. Branch cuts (`sqrt`, `ln`,
  fractional powers) break the curve where the value jumps. An equation,
  inequality, or a use of v is an error, as is a path whose split into real
  and imaginary parts is too large (deeply nested products). An expression
  that mentions i but is real for every u (`abs(exp(i u))`) is not a path
  and says so.
- Complex plot: a complex expression in `w` — `w + 4/w`.
- Domain coloring: `domain(f)` where f is complex — `domain(w^2 + 1)`. Hue is
  arg f, brightness |f|; zeros render black and poles white.
- Conformal map: `conformal(f)` — `conformal(w^2/4)` — draws the image of the
  coordinate grid under f (the level curves of re f and im f).
- Fractal: `iter(step)` iterates `z ↦ step`, coloring by escape time. Inside
  `iter` only, `z` is the iterate. If the step mentions the plane (`w`, `x`,
  `y`) iteration starts at 0, so `iter(z^2 + w)` is the Mandelbrot set; with a
  fixed constant the pixel is the starting point, so `iter(z^2 - 0.8 + 0.156i)`
  is a Julia set. An optional second argument sets the iteration count,
  `iter(z^2 + w, 600)` (default 250, max 5000). Any expression works:
  `iter((|re(z)| - i |im(z)|)^2 + w)` is the burning ship.
- Point: `(2, 3)`; animate with t: `(2cos(t), 2sin(t))`.
- Data list: `[3, 1, 4]` draws dots at (1, 3), (2, 1), (3, 4) — the panel can
  switch them to bars — and `[(1, 2), (3, 4)]` draws a scatter of points.
  Any list-valued expression plots the same way (`L^2 + 1`,
  `(L, sort(L))` with `L` defined above, or a CSV column: `person.age`).
- Geometry: with points defined in rows above (`A = (0, 0)`), `segment(A, B)`,
  `polyline(A, B, C, …)` (an open path through the points, not filled),
  `vector(A, B)` (an arrow from A to B; `vector(V)` draws V from the origin —
  the head keeps its size on screen), `line(A, B)` (the infinite line through
  A and B), `polygon(A, B, C, …)` (filled), `square(A, B)` (erected on side A→B, to the left of the direction
  A→B), and `circle(center, radius)` draw figures that follow their points. Point arithmetic works anywhere: `A + B`, `2A`,
  `(A + B)/2`, `midpoint(A, B)`, `perp(A)` (rotate 90° counterclockwise),
  `rotate(P, a)` (turn by a about the origin; `rotate(P, a, C)` about C; in
  3D `rotate(P, a, axis)`),
  `unit(A)`, `dot(A, B)`, `cross(A, B)`, `|A - B|` (distance). Measurements:
  `distance(A, B)` is `|A - B|`; `angle(A, B, C)` is the angle at B, turning
  from B→A to B→C — signed, counterclockwise positive, in radians in (−π, π]
  (`angle(A, B, C) 180/pi` for degrees; wrap it in `abs(…)` for the unsigned
  angle) — and `angle(U, V)` is the same from vector U to vector V. Both are
  plain numbers: a bare row reads out "= value" (nothing is drawn — no arc
  marker), and they work inside any expression (`circle(A, distance(A, B))`).
  A scalar list inside a point broadcasts as in any scalar math
  (`distance(A, (L, 0))` is a list of distances); a list of points as an
  argument broadcasts too, and P[1] selects a point value. An angle with a zero-length arm (`angle(A, A, C)`) reads "undefined". A parametric
  point like `A + u(B - A)` traces the segment. Tuple literals work directly
  as arguments (`segment((0, 0), A)`) and as operands: `A + (1, 2)`,
  `((0, 0) + B)/2`, `|(3, 4)|`.
- Geometry in space: `A = (1, 2, 3)` defines a 3D point with scalar
  components `A_x`, `A_y`, `A_z`. `segment`, `polyline`, `polygon`, and
  `vector` accept matching 2D or 3D points, including tuple literals. 3D
  triangles fill; larger 3D polygons draw their outline. `cross` returns a
  vector in 3D. 3D `angle` is unsigned in [0, pi]; zero arms are undefined.
  `perp`, `line`, `square`, and `circle` remain planar; 3D dragging is not defined.
- Convex hulls: `hull(A, B, C, …)` or `hull(P)` for a whole point list draws
  the convex hull — a filled polygon in 2D, a lit opaque solid with flat-shaded faces and outlined edges in 3D
  (coplanar facets merge, so a cube shows six squares). Interior and repeated
  points are ignored, the points may move with sliders or t (the hull is
  rebuilt each frame), and flat 3D input degrades to a polygon. Up to 2000
  points in 3D. Point figures transform as a whole — `R hull(P)`,
  `2 polygon(A, B, C) + (1, 0)`, `-segment(A, B)`, `rotate(hull(P), a)`,
  `e^(t cross(n)) hull(P)` — meaning the figure of the transformed points
  (the same as `hull(R P)`; works for segment, polyline, polygon, vector and
  hull). A LIST in the transform draws one figure per element:
  `th = 2pi [0..4]/5; rotate(hull(P), th)` is a rosette of 5 hulls, while
  `hull(rotate(P, th))` is the single hull of all 5 copies. Point lists also
  transform on their own: `R P`, `P + (1, 0)`, `rotate(P, a)`.
  It composes with crossing lists and rotations:
  `hull(([0,1],[0,1],[0,1]))` is a solid cube, `hull(([-1,1],[-2,2]))` a
  rectangle, and `phi = (1+sqrt(5))/2; k = 2pi [0..2]/3;
  hull(rotate((0, [-1,1], [-phi,phi]), k, (1,1,1)))` a solid icosahedron.
- Object families: a list in a scalar position draws one object per element:
  `y = [1,2,3] x`, `circle((0,0), [1,2,3])`, `(cos(u),sin(u),[1,2,3])`.
  Uses of the same named list move together (`m = [1,2]`, `y = m x + m` is 2
  lines); independent lists cross (`y = [1,2] x + [0,1]` is 4 lines,
  `circle(([0,1],[0,1]), 1)` is 4 circles). Limit 32 objects, or 8
  when the family is 3D — except point figures (segment, vector, polyline,
  polygon, hull), which are cheap and allow 1024: a deformation drawn as
  arrows is `a = [0..20]; b = [0..20]; P = (a, b);
  f(x,y) = (x + y/2, y + sin(x)/2); vector(P, f(P))`. Shader members share
  one program with per-draw uniforms. Scalar fields/domain coloring/fractals cannot superimpose as
  families; use a selected list element. Point-list arithmetic works:
  `Q = P + (1,0)`, `2P`, `P[2]`; `polyline(P)` / `polygon(P)` consume an
  entire point list, including a CSV scatter `polyline((data.x, data.y))`.
  Range bounds may use sliders, but not t; families re-expand on bound changes.
- Sequence values: `a_n = 1/n` defines `a_3` as a scalar and `a_[1..10]`
  as a list, usable in sums, scatters, histograms and regression. Indices
  must be whole numbers from 0 to 1000. Recurrences use their `a_0` seed
  (default 0.5); terms compute as a bounded chain of scalar constants, so
  sliders and t remain live. A recurrence using x as a bifurcation parameter
  cannot also supply constant terms without binding that parameter.
- Decided comparisons: `2+2=4` reads “Always true (4 = 4)”; `e=2` reads
  “Never true (2.71828 ≠ 2)”. Comparisons involving sliders/time say
  “True now” or “False now”. They draw no degenerate implicit curve.
- Vector field: a 2-tuple in x, y — `(-y, x)` — drawn as animated streamlines
  (line-integral convolution); clicking the canvas traces integral curves.
  A 3-tuple in x/y/z, or `(x',y',z') = (10(y-x), x(28-z)-y, x y-8z/3)`,
  draws 32 deterministic auto-seeded trajectories in space. Traces run in a
  worker with bounded RK4 steps and a directional fade. Moving fields are
  retraced at most four times per second. The arrows control switches to a
  5×5×5 lattice of direction glyphs; there is no ambiguous 3D click-to-seed.
- ODE / slope field: `dy/dx = x y` or `y' = sin(x) - y` plots the direction
  field (1, f); a system `(x', y') = (y, -sin(x))` plots its phase portrait.
  (Primes on x/y/z mean this. A prime on a name of your own is a state — see
  below — so `y' = -y` is a slope field while `th' = -th` is a simulation.)
- Random variable: `X ~ Normal(mean, sd)` declares X and plots its density
  curve. Parameters may use sliders and t, so `X ~ Normal(0, a)` follows the
  slider live.
- Probability: `P(X < 2)`, `P(X > 0)` or `P(0 < X < 2)` shades that area
  under X's density and shows the numeric probability (normal CDF) as a
  readout under the row.
- Expectation: `E(X)`, `E(X^2 + Y)` — the mean of any expression in random
  variables, shown as a readout under the row and marked on the density.
- Parametric curve: components in u — `(2cos(2pi u), sin(4pi u))`; three
  components make a 3D curve — `(2cos(6pi u), 2sin(6pi u), 4u - 2)`.
  A plane curve lifts the pen where it jumps (a step, a pole, a branch cut)
  rather than bridging the gap: `(u, floor(3u))`, `(4u - 2, tan(12u - 6))`.
- Parametric surface: three components in u and v —
  `(cos(2pi u)(2+cos(2pi v)), sin(2pi u)(2+cos(2pi v)), sin(2pi v))`.
- 3D surface: any equation in x, y, z.
- Surface of revolution: `revolve(f)` turns the curve y = f(x) about the
  x-axis — `revolve(sqrt(x))`, `revolve(sin(x) + 2)`, or `revolve(f)` with
  `f(x) = …` defined above. A second argument names another axis:
  `revolve(y^2, y)` revolves a profile in y about the y-axis (`z` likewise).
  The profile may use only its axis variable, plus sliders and t. It is
  exactly the implicit surface `y^2 + z^2 = f(x)^2`, so a negative f draws
  the same surface as |f|, and a piecewise f with no default bounds the
  solid: `revolve({0 < x < 3: sqrt(x)})`. It must be the whole row.
- Random variable: `X ~ Normal(mean, sd)` — also `Uniform(lo, hi)` and
  `Exponential(rate)`, with short aliases `N`, `U`, `Exp`; a bare name means
  the standard parameters (`X ~ N` is Normal(0, 1)). The row plots the exact
  density curve; parameters may use sliders and t.
- More continuous families, each with an exact density, exact `P(…)` and
  exact moments. Parameter order and meaning are fixed as follows:
  `Gamma(shape, rate)` — RATE, not scale: mean = shape/rate, and
  `Exponential(r)` is `Gamma(1, r)`; `Beta(a, b)` on [0, 1];
  `ChiSquared(df)` (aliases `ChiSq`, `Chi2`) = `Gamma(df/2, 1/2)`;
  `StudentT(df)` (alias `T`, so `X ~ T(5)`); `LogNormal(mu, sigma)` where
  ln X ~ Normal(mu, sigma); `Cauchy(location, scale)`;
  `Weibull(shape, scale)`. Only `LogNormal` and `Cauchy` have a bare standard
  form, (0, 1). Names are case-insensitive and live only right of the `~`,
  so a constant `T` or a function `gamma(x)` does not collide with them.
  Shape, rate, scale, sigma, df, a and b must be > 0: a written-out bad
  value, or a constant holding one, is an error on the row naming the
  parameter (`Gamma(shape, rate) needs shape > 0.`). Densities with a pole at
  the support edge (`Gamma` with shape < 1, `ChiSquared(1)`, `Beta` with
  a or b < 1, `Weibull` with shape < 1) draw and shade up to the pole.
- Discrete random variable: `X ~ Binomial(n, p)`, `Poisson(mean)`,
  `Geometric(p)`, `NegativeBinomial(r, p)`, `Bernoulli(p)`,
  `DiscreteUniform(a, b)` (aliases `Binom`, `Pois`, `Geom`, `NegBin`;
  case-insensitive, no bare standard form). The row draws the pmf as stems —
  a dot at (k, P(X = k)) on a thin line from the axis — at the whole numbers
  in view. Conventions, fixed: `Geometric(p)` is the TRIAL of the first
  success, support 1, 2, 3, … (mean 1/p); `NegativeBinomial(r, p)` counts
  FAILURES before the r-th success, support 0, 1, 2, … (mean r(1 − p)/p), and
  r > 0 need not be whole — so `NegativeBinomial(1, p)` is `Geometric(p)`
  shifted down by 1; `DiscreteUniform(a, b)` includes both whole-number ends,
  a ≤ b; `Bernoulli(p)` is `Binomial(1, p)`. Valid parameters: `n` a whole
  number ≥ 0; 0 ≤ p ≤ 1 for Binomial/Bernoulli (p = 0, p = 1 and n = 0 are a
  single stem of height 1, not errors); 0 < p ≤ 1 for Geometric and
  NegativeBinomial; `mean` > 0 (`Poisson(0)` is an error). A bad value —
  written out or held by a slider, e.g. n = 2.5 — is an error on the row
  naming the parameter (`Binomial(n, p) needs a whole number n ≥ 0 (n = 2.5).`);
  use `round(n)` for a slider with fractional steps. A parameter animated
  with t is not judged: at instants where it is invalid nothing is drawn.
  `P(…)` over one discrete variable with constant bounds is exact (closed-form
  cdf, any size of n or mean) and highlights the selected stems, and strict
  and non-strict bounds are different events: `P(X < 3)` stops at 2,
  `P(X <= 3)` includes 3, `P(2 < X <= 5)` is 3, 4, 5, `P(X < 2.5)` is
  `P(X <= 2)`. A bound within rounding of a whole number is that number
  (`a = 0.1*3*10` is 3), as for the parameters. Point events exist only here: `P(X = 3)` (also `==`) is that
  stem's height and `P(X != 3)` its complement; over a continuous variable
  they are an error (the value would be 0). `E(X)` is the exact closed-form
  mean, marked on the axis. Zoomed out past 1024 whole numbers in view
  (`Poisson(1000000)`, `DiscreteUniform(-1000000, 1000000)`) the stems merge
  into the filled outline of the same exact heights.
- Arithmetic over discrete variables: `S = X + Y`, `D^2`, `D / 2`, `1 / N`,
  `{X > Y: X, Y}`, a bare `X + Y` row, `E(X Y)`, `P(X + Y = 7)`, `P(X > Y)`,
  `P(X = Y)`. A variable built ONLY on discrete bases is discrete: its row
  draws a pmf as stems — never a smoothed curve — at its atoms' true
  locations, which need not be whole numbers (`D / 2` stands on the halves,
  `D^2` on the squares, `sin(D)` wherever it lands); values equal up to
  rounding are one atom (`0.1 X + 0.2 Y` at X = Y = 1 is the atom 0.3). It is
  EXACT: the joint of the independent bases is enumerated (each unbounded base
  cut where its tails hold < 1e-12; independent parts convolve, so ten dice are
  cheap), the readout is `μ = …, σ = …`, and `E(…)` prints 4 places. The joint
  is over the BASES, so dependence is kept: `X + X` is 2X (even numbers only),
  `X - X` is the atom 0, `P(X > X)` is 0, and with `A = X + Y; B = X - Y`,
  `P(A > B)` is `P(Y > 0)`. Every `P(…)` keeps its strictness, joint events
  included — ties have mass: `P(X > Y)` + `P(X = Y)` = `P(X >= Y)` — and a
  bound is matched to an atom up to rounding (`P(S <= 0.1 + 0.2)` includes the
  atom 0.3). Where g is undefined or infinite (`1 / N` at N = 0) that mass is
  no value: the readout adds `P(defined) ≈ …`, as for densities. A mean whose
  sum diverges (`2^G` over `Geometric(0.3)`) reads `no stable mean (heavy
  tails)` / median and IQR instead of a truncated sum. Closed families answer
  through the law itself, at any size: a plain sum of independent Poissons is
  Poisson, of Binomials/Bernoullis sharing one p is Binomial, of
  NegativeBinomials sharing one p is NegativeBinomial (`X + X`, `2X`, `X + 1`
  are not — they enumerate). When the joint is too large to enumerate (more
  than 131072 points in one step: `A B C` over three `Poisson(30)`, anything
  over `Geometric(0.00001)`) the row falls back to the joint sample, says so —
  `μ ≈ …, σ ≈ … (sampled)`, `P(…)` to 3 places; a sampled moment whose tail
  cannot be certified is not printed (`σ unstable`, `σ = ∞`, or median and
  IQR with `no stable mean`) — and still draws a pmf: the
  frequency of each sampled value as stems, or past 1024 distinct values a
  histogram on the values' own lattice with heavy atoms (the 0 of
  `L B`, B Bernoulli) kept as stems; values on no lattice read `(sampled: too
  many distinct values to resolve the pmf)`.
- Discrete mixed with continuous (`N ~ Poisson(3); Z ~ Normal(0, 1);
  Y = N + Z`, `N Z`, `P(N > Z)`, `E(N + Z)`) is a continuous MIXTURE: a density
  row, estimated from the joint sample like any other sampled density (an
  atom such as `N Z = 0` stays a stem). A point event needs discrete
  variables throughout: `P(Y = 3)`, `P(N = Z)`, `P(Z = 1)` are errors naming
  the continuous variable (the value would be 0).
- Derived random variable: `Y = X^2`, `S = X1 + X2` — arithmetic over
  declared random variables declares a new one, including piecewise
  conditionals: `Y = {X > 0: X^2, 1}`. Distinct names are independent, so
  `S = X1 + X2` is the convolution; the same name stays dependent, so
  `X + X` is exactly 2X. A bare expression in random variables (`X + Y` as
  its own row) plots that density without naming it. Affine forms stay
  exact: combinations of normals (`Z = (X + Y)/2`) are the exact normal, a
  scaled/shifted uniform is a uniform, and sums of uniforms (`S = X1 + X2`,
  the central-limit demo) draw their exact piecewise-polynomial convolution —
  the triangle's apex is a true corner. Independent Gamma-family terms
  (Gamma, ChiSquared, Exponential) with positive number coefficients whose
  scaled rates agree sum to the exact Gamma (`X1 + X2` of two `Gamma(·, 3)`,
  `2X`, a sum of chi-squareds), and the square of a standard normal
  (`Z ~ Normal(0, 1); Z^2`) is the exact `ChiSquared(1)`. Any affine
  combination of independent Cauchys with nonzero number coefficients is the
  exact Cauchy (locations add, scales add through |c|: `X + 1`, `-3X`,
  `X + C`; a slider coefficient estimates instead, since at 0 the variable
  is a constant), and a LogNormal times a
  positive number is the LogNormal with mu + ln c. These rows report exact
  μ and σ where those exist.
  Everything else (products, nonlinear or piecewise transforms, mixed
  families) is estimated from 131072 joint samples and reports μ, σ as `≈`.
  A variable with heavy tails — anything that passes the tail of a `Cauchy`
  or a `StudentT` with df ≤ 2 through, like `X + 1` — reports
  `median ≈ …, IQR ≈ …` instead: its σ (and for df ≤ 1 its mean) does not
  exist, and a sample estimate of it would be noise. Whether the mean
  exists is read off the tails (it does when they decay faster than 1/x,
  with a two-standard-error margin — and a wider one over a `Cauchy` or
  `StudentT(df ≤ 1)` base — so a mean that only just exists reads as
  unstable), and a mean that numeric integration certifies always counts, so
  `E(X^2)` over `StudentT(3)` is 3 although its σ is infinite, while
  `E(X^2 + Y)` over `StudentT(2)` has no value.
  Point masses are detected and drawn as probability stems (height = mass):
  `Y = {X > 0: 1, 2}` is two stems, `floor(4X)` four, and a mixed result
  like `{X > 0: X^2, 1}` draws its stem plus the continuous density.
- Probability: `P(X < 2)`, `P(-1 < X < 2)`, and bounds around one expression
  like `P(0.5 < X + Y < 1.5)` — exact (closed-form CDF or polynomial
  integration) with the area under the density shaded, whenever the bounded
  variable or expression has an exact law. Any other inequality —
  `P(Y > 0.5)` for a nonlinear Y, `P(Y > X)` — is estimated from the joint
  samples; the row shows the value as `≈`.
- Expectation: `E(X)`, `E(2X + 3)`, `E(X^2 + Y)` — the mean of any
  expression in the declared variables, exact whenever the expression has an
  exact law (closed-form pdfs, affine combinations of normals, uniform sums)
  and the finite-sample mean otherwise. The row shows the value as a readout
  and draws a vertical marker at x = E under the expression's density. Where
  the mean does not exist (`E(X)` for a Cauchy or `StudentT(1)`, `E(X^2)`
  for a Cauchy) the readout is `no stable mean (heavy tails)` and no marker
  is drawn.

## Definitions (extra rows that set up the others)

- Constant: `a = 2` — the UI shows a draggable slider for it. Computed
  constants allowed: `b = a^2 + t`.
- Function: `f(x) = x^3 - a x` — call it from other rows; calls inline
  symbolically, so `d/dx f(x)` works. A function of several variables also
  takes ONE point in their place: with `f(x,y) = (x + y/2, y)` and
  `A = (1, 2)`, `f(A)` is `f(A_x, A_y)`, and so is any computed point —
  `f(R A)`, `f(A + B)`, `f(rotate(A, 1))`, `f(f(A))`. A list of points maps:
  `P = (a, b); f(P)` is the image of the whole lattice (one point per point
  of P), so `vector(P, f(P))`, `hull(f(Q))`, `f(R P)` and `f(P + (1, 0))`
  all work; a scalar `g(x,y) = x y` gives `g(A)` a number (`y = g(A) x`) and
  `g(P)` a value list. The point must have exactly as many components as the
  function has parameters (3D: `F(x,y,z)` takes `F(B)`). A matrix argument
  is read as the list of its rows (a named list of 2 points in 2D is a 2×2
  matrix, and `g(S)` maps it like any other point list). A point does not
  spread among other arguments, and built-ins (`atan2(A)`) do not take one.
- Point: `A = (0, 0)` — a named, labeled point drawn on the canvas; the user
  can drag it and every row using it follows. Points may be computed from
  other points defined above: `C = B + D`, `M = midpoint(A, B)`,
  `P = midpoint(A, B) - perp(B - A)/2`. Scalars over points work too:
  `n = cross(B, D)`, `L = |B - A|`. A named vector that uses x, y, or z is
  a vector field, not a pinned point: `s = (x, y)` is the position vector
  and draws nothing of its own; later rows treat it as a point, so
  `dot(s, s) = 1` and `|s| = 1` are the unit circle, `|s| < 1` the unit
  disk. `F = (y, -x)` is any field of the plane. In space,
  `s = (x, y, z); dot(s, s) = 1` is the unit sphere.
- Dragging: a point (named or bare) is draggable only where the drag has
  somewhere to write the new value: a coordinate that is a plain number
  (`(2, 3)`) or a bare slider name (`(a, 3)` with `a = 1` above) moves;
  computed coordinates (`C = B + D`, `(a+1, cos(t))`) are pinned, and 3D
  graphs have no point dragging. The MCP encode_graph_url result marks each
  point row with `"draggable"` — write coordinates as literals or sliders
  when the user is meant to move the point.
- State (simulation): `th' = -sin(th)` defines the time-derivative of `th`,
  integrated forward (RK4) as the graph animates, starting from `th(0) = 2`
  (0 if omitted). Everywhere else `th` then behaves exactly like a constant,
  so plot the system by drawing with it: `bob = (sin(th), -cos(th))` names
  the swinging bob and `segment((0, 0), bob)` draws its rod — points,
  segment() and polygon() all accept state-valued coordinates. Use this for
  anything with no closed form — coupled oscillators, a double pendulum,
  Lorenz.
- Vector state: a state whose derivative (or starting value) is a 2- or
  3-vector integrates componentwise: `r' = vel; vel' = -r/|r|^3;
  r(0) = (1, 0); vel(0) = (0, 1)` is an orbit. Components are `r_1`, `r_2`
  (and `r_3`) — use them anywhere a scalar goes — and the bare name `r`
  draws as a moving point and works in point arithmetic (`perp(r)`,
  `dot(r, r)`, `r/|r|`, segment endpoints). 3-component states (Lorenz) plot
  through their components.
- Data file: `person = open("people.csv", 3a7f9c1d2e4b)` names a CSV dropped
  onto the app. Drag a .csv/.tsv anywhere in the window and this row (plus a
  scatter of its first two numeric columns) is written for you; the file is
  parsed once and kept in the browser's local storage, pinned by the SHA-256
  prefix in the row. Each numeric column reads as a list under its dotted
  name (`person.age`); text columns read as lists of text, which filters can
  compare; missing cells become gaps the row's readout counts. The delimiter
  is sniffed (`,`, `;`, or a tab), a title line above the header is skipped,
  and comma-form numbers in a `;` or tab-separated column select decimal-comma
  parsing (`1,5` is 1.5). Columns without comma-form numbers keep dot decimals
  (`1.234` is 1.234). The readout reports decimal-comma interpretation.
  Because the bytes never
  enter the URL, opening the link on another device (or rendering it
  server-side, as share-link previews do) reports the data as
  device-local instead of drawing it; dropping the same file there restores
  it. The row's readout opens a preview of the first rows as they parsed.
  `adults = person[person.age >= 18]` names a filtered copy of a file (see
  Filters above). Size: a column and constant arithmetic over it (`col/2`,
  `sin(col)`, `(a, b)`, reductions, `hist`) stay a single array and plot to
  200 000 rows; combining a column with t, a slider, or a comparison needs
  one expression per row, and a row may build 100 000 of those in total —
  so `col t` reaches 100 000 rows, and each further mapped operation over
  the result spends its length again. A three-column scatter
  (`(p.x, p.y, p.z)`) draws in 3D, where each point is a sprite, so it is
  capped at 10 000 and says so rather than drawing part of the file — filter
  it first, or plot two of the columns.
- Naming a column, or arithmetic over one, names a list:
  `ages = person.age / 2`, `who = person.name`. It stays a single array, so
  rows below it (`mean(ages)`, `(ages, person.height)`) cost no more than
  reading the column directly.
- List: `L = [3, 1, 4]` or `L = [1..100]` names a data list; rows below
  broadcast over it (`L^2`, `(L, sort(L))`), reduce it (`mean(L)`), or index
  it (`L[2]`, 1-based). Elements may use constants, states, and t. A list of
  tuples names a scatter — except 2–3 equal-length tuple rows, which define
  a matrix (below).
- Matrix: `M = [(a, b), (c, d)]` (also `[[a, b], [c, d]]`, or named points
  as rows) defines a 2×2 or 3×3 matrix. `det(M)`, `trace(M)`, the matvec
  `M v`, and `solve(M, v)` (Cramer's rule) expand symbolically, so entries
  may hold sliders, t, x/y, or states, and results work anywhere:
  `(x', y') = A (x, y)` is a linear phase portrait, and
  `th' = om; om' = solve(M, f)` integrates a system written in the
  Lagrangian form M(θ)ω′ = f — the natural shape for a double pendulum.
  Matrices combine — `2 M`, `M / 2`, `M + N`, `M N`, `M^2` (whole powers 0–8),
  `M^-1` — and a row may name the result (`R = M N`), but a matrix is never
  plotted on its own: apply it to a vector. Note `a/2 J` reads as `a/(2 J)`;
  write `(a/2) J`.
- Rotations are matrix exponentials: with `J = [(0, -1), (1, 0)]`,
  `e^(a J) P` (or `exp(a J) P`) turns P by a, and in 3D `e^(a cross(n)) P`
  turns P by a about the axis n — `cross(n)` alone is the matrix of
  `v -> n × v`, scaled so |n| multiplies the angle (`rotate(P, a, n)`
  normalizes n for you). Both expand to exact cos/sin closed forms, so a may
  be a slider, t, or a LIST: `th = 2pi [0..4]/5; polygon(e^(th J) (1, 0))` is
  a regular pentagon, `segment((0,0), rotate((1,0), th))` its 5 spokes, and
  `phi = (1+sqrt(5))/2; k = 2pi [0..2]/3;
  rotate((0, [-1,1], [-phi,phi]), k, (1,1,1))` the 12 vertices of an
  icosahedron (independent lists cross, the named turn list zips). `e^M` works
  for ANY 2×2 — `e^(t A) (x0, y0)` is the exact solution of
  `(x', y') = A (x, y)`, spiral, saddle or node — but a 3×3 must be a rotation
  generator (skew-symmetric). Name states for what they are (`th`/`om` for angle and angular
  velocity, `pos`/`vel` for a spring — the plane's own x/y/z are reserved). A
  derivative may use t, constants and other states (not x/y); an initial
  value must be constant, and gets a slider that relaunches the run when
  dragged. A ↻ button in the panel restarts it.
- Coordinate field: a definition that depends on the plane, e.g.
  `r = sqrt(x^2+y^2); theta = atan2(y,x)`. Its level sets draw as a grid, and
  later rows may use it: adding `r = 2(1 + cos(theta))` plots a cardioid in
  polar coordinates. A named pair or triple that uses x, y, or z is a
  vector field (see Point), not a grid.
- Coordinate fields in space: a field may use z, e.g. spherical
  `rho = sqrt(x^2+y^2+z^2); theta = atan2(y,x); phi = acos(z/rho)`. A z-using
  field only defines — it draws no grid and does not by itself make the graph
  3D; rows using it do: `rho = 2` is a sphere, `phi = pi/4` a cone,
  `rho = 1 + cos(3 theta)` a surface. Fields may be built from fields
  (`rho = sqrt(r^2 + z^2)`). Cylindrical needs no new field: r, theta and z.
  Angle surfaces cut at the branch of atan2: `theta = pi/4` is a half-plane,
  and a surface lying exactly on the cut (`theta = pi`) does not draw in 3D.
  `(rho, theta, phi) = (2, pi/4, pi/3)` is the point with those three
  coordinates (three coordinates in space, two in the plane; with a z-using
  field, two constraints trace a space curve). Only atan2-valued coordinates
  wrap modulo 2pi — an acos-valued polar angle lives in [0, pi] and does not.
  Where a chart is singular (theta on the z-axis) no point is reported.
  `(rho, theta, phi) = (2, 6 pi u, pi u)` traces a space curve in the chart.
  Points in space are not draggable. Three primed coordinates define a 3D
  chart flow, e.g. `(rho',theta',phi') = (0,1,0)`, transformed by the
  symbolic 3×3 Jacobian and drawn with trajectories or arrow glyphs.
- Coordinate points: after defining r and theta above, `(r, theta) = (2, pi/4)`
  draws the point satisfying both coordinates. Angles in atan2-valued left
  components wrap modulo 2pi, so 9pi/4 and pi/4 name the same position.
  Bare tuples remain Cartesian. Non-injective charts draw multiple points:
  `p = x y; q = (x^2-y^2)/2; (p, q) = (1, 0)` draws two.
  In 2D, RHS literals or slider names can be dragged; the pointer position
  is evaluated in the named coordinates and written back to those values.
- Parametric systems: `(r, theta) = (3u, 6pi u)` traces a three-turn spiral,
  u in [0,1]. General square systems with u work too, including in 3D.
  A bounded numerical continuation follows branches, periodically searches
  for others, and breaks lines at missing/discontinuous solutions. Small
  branches can be missed; this is not certified exhaustive tracing.
- Coordinate flows: `(r', theta') = (r(1-r), 1)` draws a polar limit cycle.
  Both distinct coordinate names must have primes. Velocities are transformed
  through the coordinate Jacobian; singular chart locations are undefined.
  In 2D, click to trace trajectories just as for Cartesian vector fields. A lone
  prime on a field is invalid; an otherwise unbound name still defines a state.
  Static previews draw direction arrows for vector fields and points/curves
  for coordinate and complex-root systems.

- Surface intersections: `(x^2+y^2+z^2,z) = (9,1)` traces the circle where
  the sphere meets the plane. Bounded predictor–corrector continuation
  follows the Jacobian null space, with at most 24 branches and 512 steps
  per direction. Singularities stop a trace; tiny or isolated branches may
  be missed. This numerical tracing does not certify branch completeness.
- Certified point solving: a square system's “certify search box” control
  runs outward-rounded interval subdivision and Krawczyk tests in the worker.
  Its readout distinguishes proved roots, unresolved regions, and completeness
  of that finite search box. Existing numeric points remain visible.
  Certification currently supports finite constants, +, −, ×, / away from
  zero denominators, and integer powers up to 32; other functions, singular
  roots and exhausted budgets remain unresolved. Wrapped angular systems
  are excluded. Proofs concern real arithmetic on the input binary64 literals
  and parameter values, not a claim of completeness over all space.

## Viewport rows (framing)

- `view(x = -10..10, y = -1..1, ratio = 5)` uses five times as many pixels
  per y unit as per x unit. `ratio` must be positive and defaults to 1;
  bounds are fitted while preserving this ratio across screen sizes.
  Option/Alt + drag scales axes independently and creates/updates this row.
  Set `ratio = 1` to restore equal axis units.
- `view(x = -5..5, y = -2..2)` sets the 2D window; either axis alone works
  (`view(x = 0..0.01)`), the other is centered at 0 and sized by the canvas
  aspect. The whole box is always visible (uniform scale).
- `camera(theta, phi, radius, (tx, ty, tz))` aims the 3D orbit camera —
  angles in radians (`camera(-pi/3, 0.6)`), radius and target optional.
- Binding is two-way in the app: panning, zooming, or orbiting rewrites the
  row the way dragging a slider rewrites its constant, so the URL always
  names the exact picture on screen. Without a viewport row, interaction
  stays ephemeral and the default framing applies.
- Bounds and angles may use `pi` and defined constants, evaluated at load.
- The /g/ preview card honors these
  rows, so they show the framing you chose.

## Guidance for assistants

- To show a user a graph, emit a single https://equation.io/g/... link built
  as above; the graph renders immediately on load, no interaction needed.
- To edit a graph the user already has, decode their link (or call the MCP
  `decode_graph_url` tool), transform the rows, and emit a new link. Prefer
  restructuring into named definitions: e.g. given `y = x^2 - 2x`, "add a
  tangent line at x = 3" becomes
  `f(x) = x^2 - 2x; g(x) = d/dx f(x); a = 3; y = f(x); y = f(a) + g(a)(x - a)`
  — with `a` as a draggable slider so the point of tangency stays explorable.
- Prefer a slider constant (`a = 1; ...`) when the user may want to explore a
  parameter, and `t` when they ask for animation.
- The graph state lives entirely in the link, and the link is the interface.
  The only server endpoints are /mcp (an MCP server that validates equations
  and emits links) and /api/og (link-preview images).
- If you are unsure whether a form is supported, emit the link anyway.
  Unsupported input fails loudly and visibly (an inline error on the offending
  row), never silently — so a graph either renders correctly or tells the user
  what to fix. There is no wrong-but-silent output to worry about.

## More

- [App](https://equation.io/): the grapher itself
- [About](https://equation.io/about/): feature gallery with screenshots
- [Implicit grapher](https://equation.io/implicit/): plot an equation without solving for y
- [Slope field calculator](https://equation.io/slope-field/): y′ = f(x,y), click to trace
- [Complex function plotter](https://equation.io/complex/): f(w), domain coloring, conformal maps
- [Source code](https://github.com/aantthony/equation.io): open source (MIT); bug reports via GitHub issues
- [robots.txt](https://equation.io/robots.txt): crawling policy

## Regression

Write `X = [0,1,2,3]; Y = [1,3,5,7]; Y ~ m X + b` to fit a line.
The left side is a numeric list (defined above the fit), an explicit list,
or a CSV column. The right side is a model over equally long lists, or a
scalar broadcast to the observations. Unbound scalar names are fitted
coefficients; already defined constants stay fixed. Data and fixed constants
must be static and defined above the fit. Coefficients are reusable values:
`y = m x + b` draws the model, `f(x) = m x + b` names it, and
`(X, Y - f(X))` draws residuals. No fit settings exist outside the equations.

Examples: `Y ~ a X^2 + b X + c` (polynomial), `Y ~ a exp(b X)` (nonlinear),
`data.height ~ m data.age + b` (CSV). A fit row reports coefficients, RMSE,
R² when the observations have nonzero variance, and observation count.
Changing fixed constants or data refits the model. Missing/nonfinite pairs
are skipped with a visible count. Unequal lengths, rank-deficient models,
and too few finite observations are errors. At most 8 coefficients and
10,000 observations, or 2,000 observations for nonlinear models.
Nonlinear models must be differentiable in fitted coefficients; bounded,
deterministic multistart fitting reports a local fit or an error, not a
guaranteed global optimum. Fitting does not depend on t or simulation states.

`X ~ Normal(...)`, `X ~ Uniform(...)`, `X ~ Exponential(...)`, and the
unambiguous family names (`ChiSquared`, `StudentT`, `LogNormal`, `Cauchy`,
`Weibull`) retain their random-variable meanings. `Gamma`, `Beta` and `T`
follow the `exp` rule below — over a declared data list they are a model
(`Y ~ gamma(a X)`, `Y ~ beta (X - 1)`); with an undeclared left-hand name
they declare a random variable. Bare distribution aliases are case-insensitive,
including `exp`/`EXP`. For a declared data list, `Y ~ exp(a X)` is an
exponential regression model in any capitalization; with an undeclared
left-hand name, `Z ~ exp(2)` declares an exponential random variable.
Missing CSV data stays device-local in MCP and
previews, including curves that depend on its fitted coefficients. Fits over
literal data and their model curves work in shared links and static previews.

## Motion trails

`trail(A)` leaves a live trail of a point's observed positions, with a dot at
its current position. Use it as a whole row. Examples:

    A = (cos(t), sin(t))
    trail(A)

    trail((cos(t), sin(t), t/5))

A named 2D point, point arithmetic such as `trail(2A)`, or a 2D/3D vector state
can be traced. Components may use constants, sliders, states, and `t`; they
cannot use free x/y/z or u/v. `trail(cos(t), sin(t))` is equivalent to the
parenthesized tuple spelling. Matrix `trace(M)` keeps its existing meaning
(the sum of the diagonal), including inside other expressions.

The live browser retains at most 30 seconds and 2048 samples per trail,
sampling at most 60 times a second. Undefined positions or pauses over half a
second break the line instead of joining unobserved motion. Editing equations
or restarting the simulation clears history; framing/comment edits preserve
it. Trails start afresh on opening a graph and are not saved in shared URLs.
The MCP classifies these rows as `trail`; static previews use the fallback
card because a still image cannot reproduce accumulated live history.
